# import os
# import asyncio
# import requests


async def status():
    return
    # while True:
    #     # Call out to a heartbeat URL to let them know I'm still cookin
    #     requests.head(os.environ["HEARTBEAT_URL"], timeout=5)

    #     # Wait for 30 seconds before running the status report again
    #     await asyncio.sleep(30)
